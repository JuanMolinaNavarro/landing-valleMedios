# Puerto publico del proxy nginx en el host Ubuntu.
APP_PORT=80

# URL base embebida en el frontend al hacer build.
# /api funciona con el proxy nginx incluido en docker-compose.
PUBLIC_API_BASE=/api

# Mantener true en servidores sin sandbox de Chrome habilitado.
PUPPETEER_NO_SANDBOX=true
